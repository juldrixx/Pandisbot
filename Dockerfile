# Dockerfile

FROM node:latest

ARG APIKEY_RIOT
ARG TOKEN_DISCORD

ENV API_KEY_RIOT=$APIKEY_RIOT
ENV TOKEN_DISCORD=$TOKEN_DISCORD
ENV TZ Europe/Paris

WORKDIR /app

COPY . .

RUN npm install

ENTRYPOINT ["npm", "start"]