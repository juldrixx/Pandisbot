# Dockerfile

FROM node:latest

ARG APIKEY_RIOT
ARG TOKEN_DISCORD

ENV API_KEY_RIOT=$APIKEY_RIOT
ENV TOKEN_DISCORD=$APIKEY_RIOT
ENV TZ Europe/Paris

WORKDIR /app

COPY . .

RUN npm install

ENTRYPOINT ["npm", "start"]