# Dockerfile

FROM node:latest

# ARG TOKEN_DISCORD
ARG API_KEY_RIOT_DISCORD

ENV TOKEN $TOKEN_DISCORD
ENV API_KEY_RIOT $API_KEY_RIOT_DISCORD
ENV TZ Europe/Paris

WORKDIR /app

COPY . .

RUN npm install

ENTRYPOINT ["npm", "start"]